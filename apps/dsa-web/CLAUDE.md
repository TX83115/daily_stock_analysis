# Web 前端改动验证（从 AGENTS.md §6 迁出）

- 默认执行：`cd apps/dsa-web && npm ci && npm run lint && npm run build`
- 若涉及 API 联调、路由、状态管理、Markdown/图表渲染或认证状态，交付说明中要明确说明联动面和未覆盖风险。

其余协作规则（commit 规范、PR 流程、验证矩阵总表等）以仓库根目录 `AGENTS.md` 为准。
